import React from 'react';

import './style.css';

/**
 * A Divider for marking the end of a section.
 * @returns {element} <Divider>
 */
const Divider = ({ className, children, ...props }) => {
  return (
    <span className='Divider'></span>
  );
};
export default Divider;
