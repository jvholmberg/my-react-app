import React from 'react';
import { Container, Section } from '../../Components';

const Dashboard = ()  => (
  <Container full>
    <Section>

    </Section>
  </Container>
);
export default Dashboard;
